# When modifying this file, make sure to update on all repositories.  https://github.com/gruntwork-io/git-xargs can help with this.

name: Roadmap Auto Commenter
on:
  issues:
    types:
      - opened
  pull_request_target:
    types:
      - opened

jobs:
  call_reusable_roadmap_auto_commenter:
    uses: Exairnous/hubs-ops/.github/workflows/reusable-roadmap-auto-commenter.yml@roadmap-auto-commenter-test-reusable
